const dataModel = {
    position: {
        timestamp: Number,
        mocked: Boolean,
        provider: String,
        coords: {
        speed: Number,
        heading: Number,
        altitude: Number,
        accuracy: Number,
        longitude: Number,
        altitudeAccuracy: Number,
        latitude: Number
        }
    },

    llTest: {
        latitude: Number,
        longitude: Number,
        verification: Number
    }

}